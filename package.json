{
  "name": "wyp-cli",
  "version": "1.1.1",
  "description": "personal node cli",
  "main": "index.js",
  "scripts": {
    "start": "tsc -p tsconfig.json -w",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -m commonjs --outDir ./lib",
    "prepublishOnly": "yarn build && np --no-cleanup --yolo --any-branch"
  },
  "bin": {
    "wyp": "bin/www"
  },
  "keywords": [
    "wyp-cli"
  ],
  "author": "sam",
  "license": "ISC",
  "private": true,
  "devDependencies": {
    "@types/node": "^14.14.7",
    "chalk": "^4.0.0",
    "commander": "^5.0.0",
    "download-git-repo": "^3.0.2",
    "handlebars": "^4.7.6",
    "inquirer": "^7.1.0",
    "log-symbols": "^3.0.0",
    "np": "^6.2.1",
    "ora": "^4.0.3"
  }
}
